# Required python modules for this project.
# To install all requirement, run
#    pip install -r requirements.txt
#
# also requires pycurl module, which is better installed globally using
#
# sudo apt-get install python-pycurl
#
# or, alternatively,
# sudo apt-get install libcurl4-gnutls-dev
# pip install pycurl


thrift>=0.9.1
# installed in biothings...
#tornado>=3.2,<5
# still necessary?
tornadoes>=1.4.1
gitpython

# biothings
git+https://github.com/biothings/biothings.api.git@a4b54d796c43fce3f0f89d8b8c840c8162ff19ca#egg=biothings

#for build sphinx docs
sphinx==1.5.6
#sphinx-bootstrap-theme==0.3.1

#for nosetests
nose>=1.3.0
httplib2>=0.8

#for internal API-API querying (mygene-myspecies)
requests>=2.9.1,<2.13

#Optional
msgpack-python
boto
raven

# temp
pymongo
